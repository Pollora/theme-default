/**
 * Frontend script for the pollora-starter/hero block.
 *
 * This script is loaded only on the frontend when the block is present.
 *
 * @see https://developer.wordpress.org/block-editor/reference-guides/block-api/block-metadata/#view-script
 */

document.addEventListener('DOMContentLoaded', () => {
    const blocks = document.querySelectorAll('.wp-block-pollora-starter-hero');

    blocks.forEach((block) => {
        // Frontend interactivity here
    });
});
